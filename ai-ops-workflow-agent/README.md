# AI Operations Workflow Agent

A small, auditable AI-assisted workflow demo for risk/support queues.

The script classifies inbound cases, assigns priority, drafts reviewer notes and keeps the decision rationale visible. It is intentionally lightweight so a recruiter can read the logic quickly.

Run:

```bash
python3 workflow_agent.py
```
